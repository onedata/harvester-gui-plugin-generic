{{!-- <input
  type="text"
  value={{this.query}}
  {{on "input" this.queryChanged}}
  {{on "keydown" (invoke-on-enter @performQuery)}}
>
<button {{on "click" @performQuery}}>Query</button> --}}
<div class="query-builder">
  <div class="form-control query-builder-input">
    <QueryBuilder::SingleSlotBlock
      @queryBlock={{this.rootQueryBlock}}
      @indexProperties={{@indexProperties}}
      class="root-block"/>
  </div>
  <button {{on "click" this.performQuery}} class="submit-query">Query</button>
  {{yield}}
</div>
