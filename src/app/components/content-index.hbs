<QueryBuilder
  @query={{this.query}}
  @filteredProperties={{this.filteredProperties}}
  @queryChanged={{this.queryChanged}}
  @onPerformQuery={{this.performQuery}}
  @onGenerateCurl={{this.getQueryCurl}}
  @index={{this.index}}
/>
<QueryResults
  @queryResults={{this.queryResults}}
  @activePageNumber={{this.activePageNumber}}
  @pageSize={{this.pageSize}}
  @onFilteredPropertiesChange={{this.filteredPropertiesChange}}
  @onPageChange={{this.pageChange}}
  @onPageSizeChange={{this.pageSizeChange}}
/>
{{yield}}
