<QueryBuilder
  @query={{this.query}}
  @queryChanged={{this.queryChanged}}
  @onPerformQuery={{this.performQuery}}
  @index={{this.index}}
/>
<QueryResults @queryResults={{this.queryResults}}/>
{{yield}}
